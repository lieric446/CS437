zip file has full flutter app, dart pages (main, MainPage, and AppPage) are the one's we modified from the example

commands for flutter
flutter pub get
flutter clean
flutter run


for server just run the two server files
